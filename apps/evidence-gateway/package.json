{
  "name": "hrp-evidence-gateway",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css app.js gateway-client.js dist/",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
